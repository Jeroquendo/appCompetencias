import { TipsDirective } from './tips.directive';

describe('TipsDirective', () => {
  it('should create an instance', () => {
    const directive = new TipsDirective();
    expect(directive).toBeTruthy();
  });
});
